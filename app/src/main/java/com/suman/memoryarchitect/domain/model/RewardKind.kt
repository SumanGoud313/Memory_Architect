package com.suman.memoryarchitect.domain.model

/** What a milestone reward actually grants — see [com.suman.memoryarchitect.domain.progression.RewardCatalog]. */
enum class RewardKind {
    ROOM_THEME,
    PALETTE,
    TITLE,
    BADGE,
}