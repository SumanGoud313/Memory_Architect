{
  "name": "memory-architect-functions",
  "description": "Server-side leaderboard validation for Memory Architect - not deployed by this repo automatically. See ../LEADERBOARD_SETUP.md for how to deploy via the Firebase CLI.",
  "scripts": {
    "build": "tsc",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "googleapis": "^144.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "private": true
}
