# Firebase setup

This project ships with the full Firebase integration (Analytics, Crashlytics, Performance
Monitoring, Remote Config) already wired in code, but it stays **inert** — the app builds and
runs exactly as before — until you connect a real Firebase project. Nothing else in this project
can do that step for you: it requires your own Google account and a few clicks in the Firebase
console.

## Why the app doesn't just "have" Firebase already

Firebase identifies your app via a `google-services.json` file, generated by the Firebase console
for one specific project + one specific `applicationId`. That file isn't something that can be
fabricated locally — it has to come from a real project you own. This repo does not include one
(and it should never be committed once you add it — see below), and `app/build.gradle.kts`
conditionally skips applying the Firebase Gradle plugins when it's absent specifically so the
project keeps building cleanly without it.

## Steps

1. Go to the [Firebase console](https://console.firebase.google.com/) and create a new project
   (or pick an existing one).
2. Add an Android app to that project with application ID **`com.suman.memoryarchitect`**
   (must match `app/build.gradle.kts`'s `defaultConfig.applicationId` exactly).
3. Download the generated **`google-services.json`** and place it at:
   ```
   app/google-services.json
   ```
4. In the Firebase console, enable the products you want (Analytics is on by default when you
   create the app; Crashlytics and Performance Monitoring each have a one-click "enable" the
   first time your app sends data).
5. Add `app/google-services.json` to `.gitignore` if this project is ever put under version
   control — it's environment-specific, not something to share across machines/forks.
6. Rebuild (`./gradlew assembleDebug`). No other code changes are needed — the next build detects
   the file, applies the Firebase Gradle plugins, and every event this project already logs starts
   flowing to your project's Analytics/Crashlytics/Performance dashboards.

## What works before you do this

Everything. `FirebaseAvailability.isConfigured` (`core/analytics/FirebaseAvailability.kt`) is
`false` until the file exists, and every Firebase-backed class in `core/analytics/` checks it
before touching any `Firebase.*` API — so the app runs normally, gameplay is completely
unaffected, and analytics calls silently no-op instead of crashing.

## Known limitation: Performance Monitoring's automatic instrumentation

As of this writing, Firebase's Performance Monitoring Gradle plugin
(`com.google.firebase.firebase-perf`) does not support Android Gradle Plugin 9.x — it depends on
an AGP API (`com.android.build.api.transform.Transform`) that AGP 9 removed, and fails to apply at
all. This is an open upstream issue
([firebase-android-sdk#7293](https://github.com/firebase/firebase-android-sdk/issues/7293)), not
something fixable from this project's side by picking a different plugin version — there is no
released version that supports AGP 9 yet.

`app/build.gradle.kts` has a single `enableFirebasePerfPlugin` flag (currently `false`) gating
this plugin specifically, independent of Analytics/Crashlytics — Analytics and Crashlytics both
apply and work normally regardless of this flag. Once Firebase ships an AGP-9-compatible release,
flip that one flag to `true`.

**This does not remove Performance Monitoring entirely.** The plugin's job is *automatic*
bytecode-woven instrumentation (screen rendering time, network request timing) — without it,
those two automatic traces aren't collected. The manual custom trace this project already logs
(`level_load`, around on-device level generation — see `core/analytics/PerformanceTracer.kt`)
is a plain SDK API call (`Firebase.performance.newTrace()`) that doesn't need the plugin at all,
and works today.

## Remote Config note

`RemoteConfigRepositoryImpl` now picks between two backends per call (`activeRemoteSource()`),
the same pattern `ProgressionRepositoryImpl`/`MissionRepositoryImpl` already use for their own
dual sources:

- **`data/repository/FirebaseRemoteConfigSource.kt`** — real Firebase Remote Config, used
  automatically once this setup is complete. Currently the *only* keys actually served from it are
  the three the Seasonal Event framework reads (`event_active_id`, `event_start_epoch`,
  `event_end_epoch`) — see `LiveEventCatalog.kt`'s doc for why event windows specifically were
  chosen as the first value to migrate off the mock backend (they change often and benefit most
  from not needing a release).
- **`data/repository/MockBackendRemoteConfigSource.kt`** — the original dev mock backend
  (`mock-backend/index.js`'s `/v1/config/remote`), still authoritative for every other key
  (`ad_cadence`, `difficulty_curve_base`, and every ad key in the table below) and the fallback
  whenever Firebase isn't configured.

To actually turn an event on/off in production, define these three parameters under **Remote
Config** in the Firebase console (Engage → Remote Config) and publish a change:

| Parameter | Example value | Meaning |
| --- | --- | --- |
| `event_active_id` | `HALLOWEEN` | Must match one of `LiveEventCatalog.events`' ids, or empty for "no event." |
| `event_start_epoch` | `1761955200` | Epoch-second window start - overrides the catalog entry's own default window. |
| `event_end_epoch` | `1762819199` | Epoch-second window end. |

Leaving all three unset (or `event_active_id` empty) is exactly the same as before this setup -
`FirebaseRemoteConfigSource`'s own defaults match that inert state, so nothing breaks if you
connect Firebase without ever touching these parameters.

## Ad monetization Remote Config keys

`core/ads/AdRemoteConfig.kt` reads the following keys — same raw string map, same per-key
fallback-to-default convention as everything else on this page. **These are not yet migrated to
`FirebaseRemoteConfigSource`** (same status as `ad_cadence`/`difficulty_curve_base` above) — until
that migration happens, tuning them means editing `mock-backend/index.js`'s `remoteConfig` object
and restarting the mock backend, not the Firebase console. Migrating them is a small, mechanical
follow-up (add each key to `FirebaseRemoteConfigSource` the same way the three event keys were
added) once you want console-based tuning without a mock-backend redeploy.

| Parameter | Default | Meaning |
| --- | --- | --- |
| `emergency_ads_disabled` | `false` | Master kill switch — `true` disables banner + interstitial + rewarded everywhere, instantly, no release needed. |
| `banner_ads_enabled` | `true` | Banner ads master toggle. |
| `interstitial_ads_enabled` | `true` | Interstitial ads master toggle. |
| `rewarded_ads_enabled` | `true` | Rewarded ads master toggle. |
| `interstitial_cooldown_seconds` | `180` | Minimum real time between two interstitials. |
| `interstitial_min_level_completions_before_first` | `3` | Levels completed required since the last interstitial (also protects brand-new installs, since the baseline starts at 0). |
| `interstitial_min_session_count` | `2` | App sessions required before any interstitial can show (first-session protection). |
| `interstitial_session_cap` | `4` | Max interstitials shown in one app session. |
| `interstitial_daily_cap` | `6` | Max interstitials shown in one calendar day, independent of the session cap — several short sessions in one day can't dodge a session-only limit. |
| `interstitial_cooldown_after_rewarded_seconds` | `60` | Minimum time after any rewarded ad before an interstitial can show. |
| `reward_multiplier_enabled` | `false` | Gates the "Optional Reward Multiplier" rewarded placement. |
| `reward_multiplier_value` | `2.0` | Multiplier applied when that rewarded ad completes. |
